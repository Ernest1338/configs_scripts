FROM ubuntu:latest

MAINTAINER Ernest1338 version: 0.1

ENV DEBIAN_FRONTEND=noninteractive
RUN apt update && apt install -y fossil

EXPOSE 8080

CMD ["fossil", "server", "--create", "--user", "root", "/root/test.fossil"]
