FROM ubuntu:latest

MAINTAINER Ernest1338 version: 0.1

ENV DEBIAN_FRONTEND=noninteractive
RUN apt update && apt install -y fossil && cd ~ && fossil init --user root test.fossil && fossil server test.fossil

EXPOSE 8080

CMD ["echo SERVER STARTED"]
